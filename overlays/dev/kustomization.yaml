resources:
  - ../../base
patchesStrategicMerge:
  - deployment-patch.yaml
patchesJson6902:
  - target:
      version: v1
      kind: Deployment
      name: myapp
    path: json-patch.yaml
configMapGenerator:
  - name: app-config
    literals:
      - LOG_LEVEL=debug
      - FEATURE_X=true
secretGenerator:
  - name: app-secret
    literals:
      - DB_PASSWORD=devpassword
      - API_KEY=devkey
