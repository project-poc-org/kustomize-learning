cat > deployment.yaml << 'EOF2' && \
# Base service
cat > service.yaml << 'EOF3' && \
echo "✅ Created base resources"
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: app-config
    literals:
      - LOG_LEVEL=info
      - FEATURE_X=true
secretGenerator:
  - name: app-secret
    literals:
      - DB_PASSWORD=supersecret
      - API_KEY=devkey
  - configmap.yaml
  - secret.yaml
commonLabels:
  app: myapp
  managed-by: kustomize
